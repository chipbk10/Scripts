### Upgrade

```
brew upgrade vim
```
### Others

```
:set number # show line number
0 # goto the beginning of a line
$ # goto the end of a line
w,e # goto the beginning|end of a word in the forward direction
b # goto the beginning|end of a word in the backward direction
[[ # goto the beginning of the file
]] # goto the end of the file
```
